📦 У меня установлен кастомный WordPress-плагин `Fish Password API`.

Он добавляет JWT-защищённый REST API для смены пароля:
POST /wp-json/fish/v1/change-password

🔁 Сейчас я хочу обновить плагин вручную.

📌 Что тебе нужно знать:

- Плагин не использует базу данных (стейт не сохраняется)
- Ничего не ломает и не зависит от других плагинов
- Я скидываю архив zip, в котором лежит ПРЕДЫДУЩАЯ ВЕРСИЯ плагина
- Тебе нужно обновить код и сгенерировать новый `.zip` (уже с обновлением)
- Я потом загружу этот новый zip в WordPress

🛠️ Шаги, которые я сам выполню в админке WP:

1. Деактивирую текущий плагин
2. Удаляю его через "Плагины"
3. Загружаю новый `.zip` через "Добавить → Загрузить"
4. Активирую обновлённую версию

✅ Важно: обновление должно быть полностью безопасным (никаких потерь)

📩 Жду от тебя:
- Новую версию zip (fish-password-api.zip)
- Обновлённый `Version:` в комментарии PHP-файла

🕒 Как часто обновлять?
🔁 Плагин не требует регулярных обновлений.
Обновляй только если:

Хочешь изменить поведение (валидация, логика, структура)

Добавляешь новые поля (например, confirm_password)

Обновляешь безопасность (в будущем, если появятся рекомендации)

🗓 Рекомендация: Проверять 1 раз в 3–6 месяцев, или если что-то поменялось на стороне JWT/авторизации.